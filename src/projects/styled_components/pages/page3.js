import React from "react";

export const Page3 = (props) => {
  return (
    <div style={{ paddingTop: "60px" }}>
      "In the beginning there was the void, shapless, formless, the darkness
      that holds all light."
    </div>
  );
};
